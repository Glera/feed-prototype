{
  "name": "feed-prototype",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Swipe platform (Telegram Mini App): vertical feed of playable mini-games with a social layer (stars, series, challenges) and generative UGC meta experiments. See README.md and ISLAND.md.",
  "scripts": {
    "dev": "vite",
    "lint": "tsc --noEmit && npm run check:rewards && npm run check:series && npm run check:dwell && npm run check:cp-outbox && npm run check:run-ticket-outbox && npm run check:result-receipts && npm run check:catalog-player-v2 && npm run check:catalog-feed-authority",
    "check:rewards": "node scripts/check-reward-vectors.mjs",
    "check:series": "node scripts/check-series-policy.mjs",
    "check:dwell": "node scripts/check-active-dwell.mjs",
    "check:cp-outbox": "node scripts/check-control-plane-outbox.mjs",
    "check:run-ticket-outbox": "node scripts/check-run-ticket-start-outbox.mjs",
    "check:result-receipts": "node scripts/check-result-receipts.mjs",
    "check:catalog-player-v2": "node scripts/check-catalog-player-v2.mjs",
    "check:catalog-feed-authority": "node scripts/check-catalog-feed-authority.mjs",
    "serve:catalog-player-v2-browser": "node scripts/serve-catalog-player-v2-browser-harness.mjs",
    "check:cp-browser": "node scripts/check-control-plane-feed-browser.mjs",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "verify:island": "npm run lint && vite build && npm --prefix ../swipe-generator run check && npm --prefix ../swipe-ugc run verify"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.110.0",
    "typescript": "^5.5.4",
    "vite": "^5.4.0",
    "vite-plugin-singlefile": "^2.0.0"
  }
}
