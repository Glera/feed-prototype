# Island — мета-эксперимент «остров» (AI UGC-механики)

Параллельный мета-эксперимент свайп-платформы: игрок **генерирует собственные
варианты механик** по текстовому промпту и выставляет их на своём острове.
Живёт за иконкой-**треугольником** на нижней панели фида. (Вторая, независимая
мета — «Creator District» за ромбом — здесь не описывается; это разные
эксперименты, у каждого свой неймспейс.)

## Концепт: остров-лоскут

Остров — витрина творчества игрока, а не декорация из каталога:

- Каждая созданная механика — **здание**, и оно **красит свой сектор** острова:
  земля, пропсы и палитра берутся из сгенерированной темы. Биом не выбирается —
  он складывается из того, что игрок создал.
- **4 фиксированных слота = кап.** Новая механика на занятом слоте перезатирает
  старую (Rebuild с предупреждением — плеи и лайки сгорают). Слот — дефицит.
- **Гости** играют механики хозяина (повышенная награда) и могут **лайкнуть
  только после победы** — лайк по построению завязан на реальное вовлечение.
- Механика игрока — это **рецепт** (базовый плейбл + правки), а не копия билда.
  Сегодня рецепт = подмена палитры marble-sort; дальше — арт-паки и правки
  логики с валидацией.

## Карта системы (4 репозитория)

| Репо | Роль | Ключевые файлы |
|---|---|---|
| `feed-prototype` | UI острова + клиентский форк + dev-эндпоинты | `src/island.ts` (весь UI/стейт/форк, стили инжектятся, неймспейс `isl-`), `src/feed.ts` (хук: иконка `triangle` → `openIslandWorld`, ~20 строк), `src/api.ts` (`apiIslandTheme`/`apiIslandBake`), `vite.config.ts` (`islandThemeApi`: dev-эндпоинты `/island-api/*`, раздача `/ugc/*`) |
| `swipe-backend` | Продакшн-эндпоинты + bake-runtime | `app/api_island.py` (`/api/island/theme`, `/api/island/bake`), `app/config.py` (island-настройки), `Dockerfile.bake` (node 22 + playwright + chromium), `scripts/start-bake-runtime.sh` (клоны, сборка базы, ready/error-файлы; статус в `/health`) |
| `swipe-ugc` | Хостинг артефактов + воркер | `u/<user>/<slug>-<hash8>.{html,payload.js}` (иммутабельные артефакты), `worker/bake.mjs` (bake → тест → publish → notify), `render.yaml` |
| `playables` | Базовые билды для рецептов | `marble-sort-swipe/dist-swipe/` (шелл + payload; палитра шариков = 6 hex-констант в payload) |

## Поток: создание механики

```
шаблон → промпт → ГЕНЕРАЦИЯ ТЕМЫ → превью (реролл: 1 бесплатный, дальше 30 🧩)
   → Build → BAKE-ON-CONFIRM → hosted URL → бот-пуш игроку
```

### Генерация темы (тема-пак)

Тема-пак: `{name, items[6] (цвета шариков), ground, edge, boardBg, body, roof, prop}`.

Клиент (`aiTheme` в island.ts) идёт по лесенке фоллбэков — генерация **никогда
не блокирует игру**:

1. **Бэкенд** `POST /api/island/theme` (TMA-auth): Anthropic API
   (`island_theme_model`, по умолчанию `claude-opus-4-8`), pydantic-валидация —
   hex-поля, prop из белого списка и **попарная RGB-дистанция шариков ≥ 90**
   (различимость = геймплейное требование). Невалидный пак → 1 корректирующий
   ретрай с текстом ошибки, второй провал → 422.
2. **Dev-vite** `POST /island-api/theme` (localhost): та же схема; при отсутствии
   `ANTHROPIC_API_KEY` зовёт локальный Claude Code (`claude -p`) — покрывается
   подпиской разработчика (только dev-машина).
3. **Keyword-пресеты** (5 захардкоженных паков) — если оба API недоступны;
   в превью честная метка `preset theme (AI offline)`.

Асинхронный UX: из шита генерации можно уйти («Keep browsing») — слот
показывает стройку 🏗️, по готовности механика строится сама + тост. Гонки
двойной генерации на слот отсекает `generationBySlot`.

### Bake-on-confirm (публикация)

Печём только по подтверждению постройки (превью/рероллы артефактов не
порождают). `POST /api/island/bake` → бэкенд запускает `worker/bake.mjs`
(сериализовано `_bake_lock`; `--user` и `--chat` = `caller.id` из TMA — клиент
не может назваться чужим):

1. **bake** — читает базовый билд, подменяет 6 констант палитры в payload,
   пишет `u/<user>/<slug>-<hash8>.*` (hash содержимого = иммутабельность,
   кэш навсегда, «новая версия» = новый файл);
2. **тест** — headless chromium (playwright): дефолтный гейт «загрузилась +
   8 с без ошибок», `UGC_FULL_WIN=1` — полная победа автоплеем; провал →
   ничего не коммитится, артефакты удаляются;
3. **publish** — `git commit` + `push` в swipe-ugc (Render автодеплоит ~1 мин);
4. **notify** — `sendMessage` игроку от бота («сгенерирована, протестирована,
   опубликована» + ссылка);
5. бэкенд ждёт доступности URL (до `island_deploy_wait_sec`=90 с) → ответ
   `{rel, url, ready}`; `ready:false` → тост «hosting is warming up».

URL выдаётся игроку **строго после push** — любая аварийная ветка оставляет
здание на клиентском форке, битых ссылок не бывает. Bake-runtime на Render
готовится фоном при старте API (клоны swipe-ugc и playables по GIT-токену,
`npm ci`, **сборка marble-sort-swipe из исходников** — база всегда свежая);
статус — `bake_runtime` в `/health`.

### Играние (лесенка приоритетов)

`playSeries` выбирает источник: **HOSTED** (артефакт из swipe-ugc; прошёл
автоплей-гейт, watchdog не нужен) → **FORK** (клиентский форк на лету:
fetch базы → подмена палитры → `doc.write` в about:blank-фрейм; стражи:
stale-check констант, watchdog «нет канваса за 5 с → сток», любой сбой → сток)
→ **STOCK** (обычный билд платформы). Победа ловится по `postMessage
{source:'playable', type:'completed', success}` — тем же сигналом, что у фида.

## Рецепт: источники истины (⚠️ дублируется)

| Что | Где живёт | Копии |
|---|---|---|
| Константы палитры `SORT_MARBLES` | `swipe-ugc/worker/bake.mjs` | `island.ts` (клиентский форк) |
| Валидация пака | `api_island.py` (pydantic) | `vite.config.ts`, `bake.mjs` |
| Промпт темы | `api_island.py` | `vite.config.ts` (**уже разошлись** — см. TODO 1) |

До выделения единого источника: **канон — бэкенд** (`api_island.py`), правки
синхронизировать руками во все копии.

## Дебаг

- Чип в шапке экрана игры: `HOSTED · <тема>` / `FORK · <тема>` / `STOCK ·
  <причина>`; тап — полный лог запуска (fetch, форма артефакта, замены палитры,
  watchdog, сообщения фрейма, исход).
- Консоль браузера: префикс `[island]`; терминал dev-сервера: `[bake]`,
  `[ugc-worker]`; бэкенд: `/health` → `bake_runtime.state`.

## Стейт (прототипный)

`localStorage["island-proto-v1"]`: `tokens`, `buildings[{slot, tpl, pack, name,
plays, likes, liked, url?}]`, `aiPacks` (AI-темы персистятся — форки переживают
перезагрузку), `aiSeq`. Стейт **клиентский** — кросс-девайс и настоящие визиты
гостей появятся с серверным стейтом (TODO 7).

## Конфиг

| Где | Переменная | Смысл |
|---|---|---|
| клиент | `VITE_API_BASE` | база swipe-backend (иначе прод-URL по умолчанию) |
| vite dev | `ANTHROPIC_API_KEY` / `ANTHROPIC_AUTH_TOKEN` | API-путь генерации; без них — `claude -p` под подпиской |
| vite dev | `UGC_BASE_URL`, `BOT_TOKEN`, `UGC_NOTIFY_CHAT_ID` | абсолютные ссылки, бот, dev-фоллбэк чата |
| backend | `ISLAND_THEME_MODEL` | модель генерации (default `claude-opus-4-8`) |
| backend | `UGC_REPO_PATH`, `PLAYABLES_ROOT_PATH`, `UGC_BASE_URL`, `BOT_TOKEN` | пути раннтайма, публичная база, бот |
| backend | `ISLAND_BAKE_TIMEOUT_SEC` (300), `ISLAND_DEPLOY_WAIT_SEC` (90), `ISLAND_DEPLOY_POLL_SEC` (3) | таймауты bake/деплой-ожидания |
| worker | `PLAYABLES_ROOT`, `UGC_FULL_WIN`, `UGC_NO_PUSH`, `BOT_TOKEN`, `UGC_NOTIFY_CHAT_ID`, `UGC_BASE_URL` | см. README swipe-ugc |

## Хостинг и приватность

Сейчас UGC хостится в **публичном** репо `github.com/Glera/swipe-ugc` +
бесплатный Render static. Для прототипа это ок (статик-хостинг всё равно
публичен, а public-репо даёт запасной CDN через jsDelivr).

**Решение на будущее: сгенерированные механики прятать.** Целевая схема —
закрытый CDN: приватный bucket (S3/R2) + отдача через CDN с непубличными /
подписанными URL, без листинга; репозиторий приватизировать или заменить
объектным хранилищем (воркер меняет только шаг publish). Причины: механики
игроков — контент продукта, не для скрейпинга и разглядывания вне платформы.
Порог, за которым переезд нужен и по нагрузке: сотни генераций в день
(git-as-storage распухает, деплой-очередь Render).

## TODO (замечания аудита 10.07.2026 + известные швы)

1. **Единый источник рецепта.** `SORT_MARBLES`, валидация пака и промпт темы
   продублированы в 4 местах; промпт в бэкенде уже отстал от vite-версии
   (потеряна строка «bend realism for gameplay»). Вынести константы+промпт в
   swipe-ugc, воркер импортирует, бэкенд читает оттуда же.
2. **Джоб-очередь для bake.** Сейчас `/api/island/bake` держит HTTP-запрос до
   300 с под глобальным `_bake_lock` — игроки в одной очереди, прокси Render
   может оборвать раньше. Перейти на 202 + очередь; URL игрок узнаёт из
   бот-пуша (уже есть) или поллинга.
3. **Dev-vite путь пометить временным.** Вне Telegram клиент после null от
   бэкенда делает лишний fetch на `/island-api/*` (404-шум на прод-статике).
   Спрятать за dev-флагом или выпилить, когда бэкенд стабилен.
4. `window.confirm` (удаление механики) → `Telegram.WebApp.showConfirm` —
   нативный confirm в TMA-вебвью капризен.
5. `checkout -B` в `start-bake-runtime.sh` при рестарте молча отбрасывает
   незапушенный коммит (безопасно — URL не выдан, но артефакт исчезает);
   логировать факт отбрасывания.
6. **Закрытый CDN для UGC** — см. «Хостинг и приватность» выше.
7. **Серверный стейт острова** (`island_*`-таблицы за фиче-флагом когорты):
   кросс-девайс, настоящие визиты гостей по ссылке, лайки/плеи на сервере.
8. **Диплинк `startapp=island`** в main.ts + `requestWriteAccess()` при первой
   генерации (право бота писать игроку).
9. Относительный hosted-URL из dev (`ugc/...`) не переключается на
   `UGC_BASE_URL` задним числом — мигрировать или хранить `rel` вместо URL.
10. **Рецепты для merge/pins** — арт в атласах, не в константах; это шаг к
    настоящим арт-пакам (image-gen: фон + спрайт-лист).
11. **Отдельный origin для UGC как песочница** — обязателен до фазы «умные
    правки логики» (сгенерированный код не должен жить на origin платформы).
12. Себестоимость генерации: `island_theme_model` конфигурируем — замерить
    haiku на качество палитр для экономии.
